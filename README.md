
  # mori mori logic

  This is a code bundle for mori mori logic. The original project is available at https://www.figma.com/design/B5Z8KviNUpIngcX85VR08U/mori-mori-logic.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  